import 'package:flutter/material.dart';

class ShowShimmerEffect extends StatelessWidget {
  const ShowShimmerEffect({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}